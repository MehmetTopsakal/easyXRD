[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "easyxrd"
version = "0.1.6"
description = "X-ray diffraction analysis tool"
readme = "README.md"
requires-python = ">=3.11"
license = {file = "LICENSE"}
authors = [{name = "Mehmet Topsakal", email = "metokal@gmail.com"}]
dependencies = [
    "numpy", "scipy", "xarray", "matplotlib", "ipython",
    "pymatgen", "pyFAI", "fabio", "pybaselines", "h5netcdf", "h5py",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3 :: Only",
]

[project.optional-dependencies]
notebook = ["jupyterlab", "ipympl", "ipywidgets"]
materials = ["mp-api"]
dev = ["build"]

[project.urls]
Homepage = "https://github.com/MehmetTopsakal/easyXRD"

[tool.setuptools]
packages = ["easyxrd"]
